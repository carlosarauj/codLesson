/* let nomes = ['carloso','daniela','lucas']

if (nomes.length <= 5){
    let nomealto = nomes.map(nome => nome.toUpperCase())
    console.log(nomealto)
} 
*/

/*
let nomes = ['carloso', 'daniela', 'lucas'];

let nomealto = nomes.map(nome => {
    if (nome.length < 5) {
        return nome.toUpperCase();
    } else {
        return nome;
    }
});

console.log(nomealto);
*/
let frase = 'voce sabia que sabia o sabia sabia assobiar'

/* let frase2 = frase.substring(0, 10)
console.log(frase2) */
 

/* let frase = 'ola mundo'

let frase2 = frase.replace('mundo', 'pessoal')
let frase3 = frase2.replace('pessoal', 'pessoal'.toLocaleUpperCase())

console.log(frase3) */
/* 
let frase2 = frase.indexOf('o')
let frase3 = frase.substring(16, 38).toUpperCase()
console.log(frase3) */
